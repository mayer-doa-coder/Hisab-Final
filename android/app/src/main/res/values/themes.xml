<?xml version="1.0" encoding="utf-8"?>
<!-- AppCompat-based theme: required because MainActivity is an AppCompatActivity,
     which is what makes per-app language switching work below API 33 (D014).
     Actual visual styling is done in Compose, not here. -->
<resources>
    <style name="Theme.Hisab" parent="Theme.AppCompat.DayNight.NoActionBar" />
</resources>
