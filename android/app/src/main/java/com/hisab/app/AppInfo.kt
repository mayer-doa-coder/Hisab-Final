package com.hisab.app

/**
 * Placeholder to prove the module actually compiles Kotlin.
 * Real app code starts at Step 20 (docs/PHASE_GUIDE.md).
 */
object AppInfo {
    const val APPLICATION_ID: String = "com.hisab.app"
}
